//----------------------------------------------------
// RQT FETCH POST AVEC LES DONNEES DU FORMULAIRE DU SWAGGER
//-----------------------------------------------------------

const init2 = "http://localhost:5678/api/users/login";

const login = document.querySelector("form");
login.addEventListener("submit", loginForm);
